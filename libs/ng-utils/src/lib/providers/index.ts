import { Provider } from '@angular/core';
import { INITIALIZABLE_SERVICES_INITIALIZER } from './initialize-services.initializer';

/**
 * @internal
 */
export const LIB_PROVIDERS: Provider[] = [INITIALIZABLE_SERVICES_INITIALIZER];
