export enum EConfigTarget {
  // AngularJSON = 'angular.json',
  PackageJSON = 'package.json',
  CustomConfigJSON = 'tsmg.conf.json',
}

// const reg = /\.?(tsmg|ts-mixin(-generator)?)(rc|(\.|-)conf(ig)?)\.json/gi;

//             /(\.)?(tsmg|ts-mixin(-generator)?)(rc|(\.|-)conf(ig)?)?(\.json)?/g;
